@title(Version)
@shorttitle(Version)

@section(1 SecFirst Description)

This command displays the version of pasc.

Example:
@longCode(
  pasc version
)

@table( @rowHead( @cell(Next @link(commandwatch Watch Command)) ) )